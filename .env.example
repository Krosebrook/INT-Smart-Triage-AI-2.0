# Environment Variables Template for INT Smart Triage AI 2.0
#
# IMPORTANT: This file is for reference only.
# DO NOT add actual values here - use Vercel environment variables instead.
#
# Configure these in your Vercel Dashboard:
# https://vercel.com/dashboard/[your-project]/settings/environment-variables

# Client-Side Variables (VITE_ prefix - exposed to browser)
# Get these from: https://supabase.com/dashboard/project/[your-project]/settings/api
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
VITE_FORECASTING_API_URL=http://localhost:8000

# Server-Side Variables (API endpoints only - NOT exposed to browser)
# Get these from: https://supabase.com/dashboard/project/[your-project]/settings/api
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
FORECASTING_SERVICE_URL=http://forecasting-service:8000

# Gemini AI Configuration (REQUIRED for LLM-powered triage - server-side only)
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Optional: Node.js Environment
NODE_ENV=production

# Security Notes:
# - Never commit actual values to version control
# - VITE_ prefixed vars are exposed to browser (use anon key only)
# - Server-side vars are secure and use service_role key for privileged operations
# - These variables are automatically encrypted in Vercel
# - RLS policies ensure database security regardless of key exposure
# - Gemini API key is used server-side only for AI triage processing
