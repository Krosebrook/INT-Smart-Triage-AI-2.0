import type { FC } from 'react';

const DashboardPage: FC = () => {
  return (
    <div>
      <h2>Live Operations Overview</h2>
      <p>Monitor triage throughput, SLA adherence, and agent load in real time.</p>
    </div>
  );
};

export default DashboardPage;
